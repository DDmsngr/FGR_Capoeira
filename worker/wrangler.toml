name = "fgr-admin-api"
main = "src/index.ts"
compatibility_date = "2025-01-01"

# Не секретные переменные — можно править здесь.
# Секреты (ADMIN_PASSWORD, GITHUB_TOKEN) задаются через `wrangler secret put`.
[vars]
GITHUB_OWNER = "DDmsngr"
GITHUB_REPO = "FGR_Capoeira"
GITHUB_BRANCH = "main"
